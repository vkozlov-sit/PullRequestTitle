{
  "name": "pr-jira-link-action",
  "version": "1.0.0",
  "description": "Normalize PR titles and add Jira links",
  "main": "index.js",
  "scripts": {
    "build": "ncc build index.js --license licenses.txt",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}
